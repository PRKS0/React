// we do this because if we have a lot of component then we import all the component here and we use in app,jsx file through this file. to make it more easier to the import export of all the component


import InputBox from "./InputBox";

export {InputBox}